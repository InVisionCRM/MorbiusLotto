/**
 * Shared PWA install UI artwork (transparent PNG — purple M + shield).
 * Path is URL-encoded for safe use in `next/image` `src`.
 */
export const PWA_INSTALL_LOGO_SRC = '/morbius/MorbiusLogo%20(3).png' as const;

export const PWA_INSTALL_LOGO_WIDTH = 960;
export const PWA_INSTALL_LOGO_HEIGHT = 888;
