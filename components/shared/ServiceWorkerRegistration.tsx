'use client';

// Serwist (@serwist/next) handles SW registration automatically at build time.
// This component is kept as a no-op placeholder so layout.tsx doesn't need editing
// if we ever need custom registration logic in the future.
export function ServiceWorkerRegistration() {
  return null;
}
